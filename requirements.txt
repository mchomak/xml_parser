# Core dependencies
aiohttp>=3.8.0
pyyaml>=6.0

# Optional: for HTML parsing fallback
beautifulsoup4>=4.12.0

# Optional: for XSD validation
lxml>=4.9.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional: for Windows .exe building
# pyinstaller>=5.0.0

# Optional: for type checking
# mypy>=1.0.0
